rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEV-ONLY permissive rules. Do NOT use in production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}